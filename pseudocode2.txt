binary search
var: 
begin
read (tab[]) (PS: the tab here is in a descending order)
n= len(tab[])
i=0
read(X)
  while (n => i)  
    if (X = tab[n/2])
        write (n/2)
        else if (X < tab[n/2]) 
          i=(n/2)+1
        else if (X > tab[n/2])
            n=(n/2)-1
end 